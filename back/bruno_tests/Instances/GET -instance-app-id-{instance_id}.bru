meta {
  name: GET /instance/app-id/{instance_id}
  type: http
  seq: 4
}

get {
  url: {{url}}/instance/app-id/{{whatif-app-id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
