meta {
  name: GET /instance/debug
  type: http
  seq: 2
}

get {
  url: {{url}}/instance/debug
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
