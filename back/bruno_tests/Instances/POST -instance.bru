meta {
  name: POST /instance
  type: http
  seq: 3
}

post {
  url: {{url}}/instance
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
